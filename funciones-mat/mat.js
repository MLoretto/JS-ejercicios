window.mathematic = {
  add : (sumador1, sumador2) => {
    let resultado = sumador1 + sumador2;
    return resultado;
  },
  multiply:(factor1, factor2) => {
    let resultado = factor1 * factor2;
    return resultado;

  }

}